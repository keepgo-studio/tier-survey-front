import Shared from '@shared';
import React from 'react'

export default function SurveyTable({
  hashedId
}: {
  hashedId: string;
}) {
  return (
    <Shared.Container>
      SurveyTable

      <p>top 100 ranked player of survey</p>
    </Shared.Container>
  )
}
